-- [[ UBY GOAT - CLEAN HEAD EDITION ]] --
-- STYLE: SAVAGE PINK | HITBOX: INVISIBLE HEAD | STATUS: PRO-SYSTEM

local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")
local players = game:GetService("Players")

-- -- 1. SCREENTROLL SYSTEM (Efek Jahil) -- --
local hijackGui = Instance.new("ScreenGui", player.PlayerGui)
hijackGui.Name = "UbyGoatSavagePink"
hijackGui.DisplayOrder = 99999
local bg = Instance.new("Frame", hijackGui)
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(255, 230, 240)
bg.Active = true

task.spawn(function()
    local colors = {Color3.fromRGB(255, 230, 240), Color3.fromRGB(255, 255, 255), Color3.fromRGB(255, 200, 220)}
    while hijackGui.Parent do bg.BackgroundColor3 = colors[math.random(1, #colors)] task.wait(0.15) end
end)

local msg = Instance.new("TextLabel", bg)
msg.Size = UDim2.new(0.8, 0, 0.4, 0)
msg.Position = UDim2.new(0.1, 0, 0.3, 0)
msg.BackgroundTransparency = 1
msg.Text = "PERANGKAT ANDA TELAH DI RETAS\nBY UBY GOAT 🖕\n\nMAMPUS GOBLOX SIH LU"
msg.TextColor3 = Color3.fromRGB(255, 50, 130)
msg.Font = Enum.Font.GothamBold
msg.TextSize = 35
msg.TextWrapped = true

local exitBtn = Instance.new("TextButton", bg)
exitBtn.Size = UDim2.new(0, 180, 0, 45)
exitBtn.Position = UDim2.new(0, 20, 1, -65) 
exitBtn.BackgroundColor3 = Color3.fromRGB(255, 120, 180)
exitBtn.Text = "KELUAR SEKARANG" 
exitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
exitBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", exitBtn).CornerRadius = UDim.new(0, 8)

exitBtn.MouseEnter:Connect(function()
    exitBtn.Position = UDim2.new(math.random(0.1, 0.8), 0, math.random(0.1, 0.8), 0)
    exitBtn.Text = "GAK BISA DI KLIK 🖕"
end)

local secretClose = Instance.new("TextButton", bg)
secretClose.Size = UDim2.new(0, 200, 0, 100)
secretClose.Position = UDim2.new(0, 0, 1, -100)
secretClose.BackgroundTransparency = 1 
secretClose.Text = "" 
secretClose.MouseButton1Click:Connect(function() hijackGui:Destroy() end)

-- -- 2. HITBOX ENGINE (Sistem Head Raksasa) -- --
_G.UBY_ENGINE = { enabled = false, currentScale = Vector3.new(1,1,1), data = {} }
local state = _G.UBY_ENGINE

local function applyHead(p)
    if p == player or not p.Character then return end
    local head = p.Character:FindFirstChild("Head")
    local hum = p.Character:FindFirstChildOfClass("Humanoid")
    if not head or not hum or hum.Health <= 0 then return end

    if not state.data[p] then
        state.data[p] = { size = head.Size, transparency = head.Transparency }
    end

    head.Size = state.currentScale
    head.Transparency = 1 -- CLEAN MODE: 100% Gak kelihatan
    head.CanCollide = false
end

local function revertHead(p)
    local saved = state.data[p]
    if saved and p.Character and p.Character:FindFirstChild("Head") then
        local head = p.Character.Head
        head.Size = saved.size
        head.Transparency = saved.transparency
    end
    state.data[p] = nil
end

-- -- 3. MENU UTAMA -- --
local screenGui = Instance.new("ScreenGui", player.PlayerGui)
screenGui.Name = "UbyGoatPinkMenu"
local main = Instance.new("Frame", screenGui)
main.Size = UDim2.new(0, 220, 0, 380)
main.Position = UDim2.new(0.1, 0, 0.4, 0)
main.BackgroundColor3 = Color3.fromRGB(255, 235, 245)
main.Draggable = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 15)

-- Tombol Yahayuk (Size 10)
local btnY = Instance.new("TextButton", main)
btnY.Size = UDim2.new(0, 180, 0, 50)
btnY.Position = UDim2.new(0.5, -90, 0.25, 0)
btnY.Text = "AUTO WALK YAHAYUK"
btnY.BackgroundColor3 = Color3.fromRGB(255, 120, 180)
btnY.MouseButton1Click:Connect(function()
    state.enabled = true
    state.currentScale = Vector3.new(10, 10, 10)
end)

-- Tombol Linz (Size 12)
local btnL = Instance.new("TextButton", main)
btnL.Size = UDim2.new(0, 180, 0, 50)
btnL.Position = UDim2.new(0.5, -90, 0.65, 0)
btnL.Text = "LINZ BAU BAWANG"
btnL.BackgroundColor3 = Color3.fromRGB(255, 160, 200)
btnL.MouseButton1Click:Connect(function()
    state.enabled = true
    state.currentScale = Vector3.new(12, 12, 12)
end)

-- -- 4. LOOP ENFORCEMENT (Biar Stabil) -- --
runService.Heartbeat:Connect(function()
    if state.enabled then
        for _, p in ipairs(players:GetPlayers()) do
            if p ~= player and p.Character then
                local hum = p.Character:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then applyHead(p) else revertHead(p) end
            end
        end
    end
end)
